using Rogue.GameObjects;
using System.Collections.Generic;

namespace Rogue.Map {
    public class RoomDecorator {
        public List<GameObject> GetDecorations(List<Room> rooms) {
            var decorations = new List<GameObject>();
            
            foreach(var room in rooms) {

            }

            return decorations;
        }
    }
}
